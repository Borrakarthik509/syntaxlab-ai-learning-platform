# Installation
1. Clone the repository: `git clone https://github.com/yourusername/SyntaxLab.git`
2. Open the directory: `cd SyntaxLab`
3. Serve locally using an HTTP server (e.g., Live Server or Python's `http.server`):
   `python -m http.server 8000`
4. Visit `http://localhost:8000`
